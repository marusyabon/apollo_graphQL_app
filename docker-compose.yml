version: "3"

services:
  server:
    build: server/
    command: "npm start"
    volumes: 
      - ./server/:/server/
    ports:
      - "8080:8080"
    depends_on:
      - mongo
    links:
      - mongo

  mongo:
    image: mongo
    restart: always
    ports: 
      - "27017:27017"
  